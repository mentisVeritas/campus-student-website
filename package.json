{
  "name": "campus-student-website",
  "version": "0.1.0",
  "private": true,
  "description": "Campus student portal — schedule, grades, attendance, documents, and campus services.",
  "scripts": {
    "install:web": "npm install --prefix apps/web",
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web",
    "db:migrate": "npm run db:migrate --prefix apps/web",
    "db:generate": "npm run db:generate --prefix apps/web",
    "db:seed": "npm run db:seed --prefix apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}

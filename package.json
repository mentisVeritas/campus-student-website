{
  "name": "campus-student-website",
  "version": "0.1.0",
  "private": true,
  "description": "Campus student portal — schedule, grades, attendance, documents, and campus services.",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev -w csw-app",
    "build": "npm run build -w csw-app",
    "start": "npm run start -w csw-app",
    "lint": "npm run lint -w csw-app",
    "db:migrate": "npm run db:migrate -w csw-app",
    "db:generate": "npm run db:generate -w csw-app",
    "db:seed": "npm run db:seed -w csw-app"
  },
  "engines": {
    "node": ">=20"
  }
}
